name: flutter6livecoordinates
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  geocoding: ^3.0.0
  geolocator: ^11.0.0
  http: ^1.2.1
  logger: ^2.2.0
  intl: ^0.19.0
  # Update to a compatible version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
